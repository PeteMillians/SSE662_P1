# To-Do List Manager

This project, module 1 of SSE 662, contains a simple command-line to-do list manager implemented in Go.


## TODO:
- Error handling
    - TaskManager.find(number): needs to throw something if that doesn't exist